name: Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Create PEM file
        run: |
          echo "${{ secrets.EC2_KEY }}" > iqbal-keypair.pem
          chmod 600 iqbal-keypair.pem

      - name: Deploy via SSH
        run: |
          scp -i iqbal-keypair.pem -o StrictHostKeyChecking=no index.html ec2-user@${{ secrets.EC2_HOST }}:/home/deploy-html-with-ga/
        # replace `ec2-user` with your actual EC2 username
